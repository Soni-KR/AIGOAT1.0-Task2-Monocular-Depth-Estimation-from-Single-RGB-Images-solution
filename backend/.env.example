# Comma-separated browser origins allowed to call the API.
AIGOAT_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Maximum accepted image upload size in whole megabytes.
AIGOAT_MAX_UPLOAD_MB=10
